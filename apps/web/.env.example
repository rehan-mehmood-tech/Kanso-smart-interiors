# Kanso web app configuration.
# Copy to .env.local and fill in. .env.local is git-ignored.

# --- Supabase -----------------------------------------------------------------
# No project exists yet. Once created:  Dashboard -> Project Settings -> API
# Both values are safe to expose to the browser; the service-role key is NOT
# and must never appear in this file.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
