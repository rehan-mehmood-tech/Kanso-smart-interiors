# Kanso API runtime dependencies.
# Install:  pip install -r requirements.txt

fastapi==0.120.4
uvicorn[standard]==0.38.0
pydantic[email]==2.12.4        # EmailStr in the schema layer
pydantic-settings==2.13.1
python-multipart==0.0.20        # multipart wall-photo uploads
httpx==0.28.1                   # outbound provider calls

# --- Supabase (Postgres + Auth + Storage) ---
supabase==2.31.0

# --- AI providers: Gemini reads the walls, Flux renders the concepts ---
google-genai==1.51.0
replicate==1.0.7
